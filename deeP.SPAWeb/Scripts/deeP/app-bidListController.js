
(function () {
    "use strict";
    angular.module("deePProperties")
        .controller("bidListController", [
            function () {
                this.bids = [{}];
                this.refreshList = function () {

                };
            }]);
})();